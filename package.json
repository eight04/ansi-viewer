{
  "title": "ANSI Viewer",
  "name": "ansi-viewer",
  "version": "1.4.0",
  "description": "View ANSI file in the browser",
  "author": "eight04 <eight04@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "bbs-reader": "^0.3.0",
    "pmore": "^0.1.1",
    "uao-js": "^0.1.1"
  },
  "scripts": {
    "test": "eslint **/*.js && web-ext lint",
    "start": "web-ext run -f \"C:\\Program Files (x86)\\Firefox Developer Edition\\firefox.exe\"",
    "preversion": "npm test",
    "postversion": "web-ext build && git push --follow-tags",
    "build-lib": "browserify -r bbs-reader -r uao-js | n-pipe background/lib.js && browserify -r pmore | n-pipe public/pmore.js && n-copy -s node_modules/bbs-reader -d public bbs-reader.css"
  },
  "devDependencies": {
    "npm-build-tools": "^2.2.5"
  }
}
