{
  "title": "ANSI Viewer",
  "name": "ansi-viewer",
  "version": "2.0.2",
  "description": "View ANSI file in the browser",
  "author": "eight04 <eight04@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "bbs-reader": "^0.3.1",
    "pmore": "^0.1.1",
    "uao-js": "^1.0.1",
    "webextension-polyfill": "^0.2.1"
  },
  "scripts": {
    "test": "eslint **/*.js --cache --ignore-pattern js && web-ext lint",
    "start": "web-ext run",
    "preversion": "npm test",
    "version": "sync-version manifest.json && git add .",
    "build": "shx rm -rf js && rollup -c && npm run build-asset",
    "build-zip": "web-ext build --ignore-files **/*.log",
    "postversion": "npm run build && npm run build-zip && git push --follow-tags",
    "build-asset": "shx mkdir -p css && shx cp node_modules/bbs-reader/bbs-reader.css css"
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "rollup": "^0.60.7",
    "rollup-plugin-cjs-es": "^0.4.0",
    "rollup-plugin-json": "^3.0.0",
    "rollup-plugin-node-resolve": "^3.3.0",
    "rollup-plugin-re": "^1.0.7",
    "shx": "^0.3.0",
    "sync-version": "^1.0.1",
    "web-ext": "^2.7.0"
  }
}
